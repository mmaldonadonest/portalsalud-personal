spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

logging:
  level:
    mx.saludocupacional.portal: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
