package mx.saludocupacional.portal.shared.audit;

/** Acciones que la bitácora es capaz de registrar. */
public enum AuditAction {
    CREATE,
    UPDATE,
    DELETE,
    EXPORT,
    IMPORT,
    LOGIN,
    LOGOUT
}
