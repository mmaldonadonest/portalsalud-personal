spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    clean-disabled: false

portal:
  jwt:
    expiration-minutes: 60

logging:
  level:
    org.testcontainers: WARN
    com.github.dockerjava: WARN
